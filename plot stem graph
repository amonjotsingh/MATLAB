plot
x(n)= 3d(n+2) + 2*d(n) + 5d(n-7), -5<=n<=15

>> for n=-5:1:15     %for loop for given range
y=3*delta(n+2) + 2*delta(n) + 5*delta(n-7);    % statement  
stem(n,y);            % plotting
hold on;    %holding plotting for current loop in dtem graph
end   %end
